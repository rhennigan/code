#ifndef LIB_HASH_
#define LIB_HASH_

#include <stdint.h>

uint64_t hash(unsigned char *str) {
    unsigned long hash = 5381;
    int c;

    while ((c = (*str++)))
        hash = ((hash << 5) + hash) + c; /* hash * 33 + c */

    return hash;
}

#endif  // LIB_HASH_
